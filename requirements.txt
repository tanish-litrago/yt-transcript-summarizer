# ─────────────────────────────────────────────────────────────────────────────
# YouTube Transcript Summarizer & Note Maker — v2.0 Requirements
# Install with:  pip install -r requirements.txt
#
# v2.0 change: BART + spaCy/TF-IDF removed. Summarization and keyword/entity
# extraction now run through Gemma 4 (e4b) served locally by Ollama.
#
# Before running this project:
#   1. Install Ollama:  https://ollama.com/download
#   2. Start the server:  ollama serve
#   3. Pull the model:    ollama pull gemma4:e4b
# ─────────────────────────────────────────────────────────────────────────────

# Gemma 4 client (talks to local Ollama server)
requests>=2.31.0

# RAG pipeline (v2.5) — ChromaDB + LangChain + Ollama embeddings
# Embeddings run through Ollama (nomic-embed-text) — no sentence-transformers needed.
# Before first use:  ollama pull nomic-embed-text
langchain>=1.0.0
langchain-community>=0.4.0
langchain-text-splitters>=0.3.0
chromadb>=0.5.0,<1.0

# Speech-to-text fallback (Whisper — GPU accelerated, kept for uncaptioned videos)
torch>=2.1.0                  # Required by Whisper only
openai-whisper>=20231117
yt-dlp>=2024.3.10

# Transcript API
youtube-transcript-api>=0.6.2

# Note export
fpdf2>=2.7.9                  # PDF export
python-docx>=1.1.0            # DOCX export

# Web UI
flask>=3.0.0

# Analytics dashboard
plotly>=5.18.0                # Interactive charts in the Analytics tab
textstat>=0.7.3               # Readability scoring

# Utilities
tqdm>=4.66.0
colorama>=0.4.6
